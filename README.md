# PortAuthenticator/ Autenticador de portas

O objetivo desse script é escanear e explorar portas que possuem permissões de login do tipo -anon, ou seja, qualquer um pode se conectar

Para executar o scrip, basta digitar em seu terminal "python3 <nome_do_arquivo.py> ip_do_host".

